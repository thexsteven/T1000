%!TEX root = ../main.tex

%%**************************************************************
%% Template for bachelor theses (etc.) of the Faculty of Technology at DHBW Stuttgart/Bad Mergentheim
%%
%% Author: Tobias Dreher, Yves Fischer
%% Date: 06.07.2011
%%
%% Author: Michael Gruben
%% Date: 15.05.2013
%%
%% Author: Ioanna Theofilou
%% Date: 10.02.2026
%%**************************************************************

\usepackage{fullpage}

\usepackage[activate]{microtype}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}

\usepackage[onehalfspacing]{setspace}

\usepackage[
  left=4cm,
  right=2cm,
  top=3cm,
  bottom=3.5cm
]{geometry}

\usepackage{makeidx}

\usepackage[english]{babel}

\usepackage[babel]{csquotes}

% Cover page
\usepackage{longtable}
\setlength{\tabcolsep}{10pt}
\renewcommand{\arraystretch}{1.5}

% Graphics
\usepackage{graphicx}

% Math
%\usepackage{amsmath}
%\usepackage{amssymb}

% Rotate page
%\usepackage{rotating}
%\usepackage{lscape}

\usepackage{color}
\definecolor{LinkColor}{rgb}{0,0,0.2}
\definecolor{ListingBackground}{rgb}{0.92,0.92,0.92}

% Working title (6-12 words, precise, without company names/abbreviations) - confirm with supervisor
\newcommand{\pdftitel}{Cycle-Overlay Visualization of Endurance Test Data for Electro-Actuators}
\newcommand{\autor}{Steven Braun}
\newcommand{\arbeit}{Bachelor's Thesis}

% For the cover page + author on the pages
\title{\titel}
\author{Steven Braun}
\date{\datum}

% PDF settings
\usepackage[%
	pdftitle={\pdftitel},
	pdfauthor={\autor},
	pdfsubject={\arbeit},
	pdfcreator={pdflatex, LaTeX with KOMA-Script},
	pdfpagemode=UseOutlines,
	pdfdisplaydoctitle=true,
	pdflang=en-US
]{hyperref}

% HTML links
\hypersetup{
	colorlinks=true,
	linkcolor=LinkColor, 
	citecolor=LinkColor,
	filecolor=LinkColor,
	menucolor=LinkColor,
	urlcolor=LinkColor,
	bookmarksnumbered=true
}

% % Harvard-style citations
% \usepackage[dcucite]{harvard}
% \renewcommand{\harvardand}{and}

% Biblatex citations
\usepackage[style=authoryear, maxcitenames=1, maxbibnames=99]{biblatex}
\addbibresource{ArbeitBib.bib}

\DefineBibliographyStrings{english}{
  andothers = {\textit{et al.}}
}

% Font
%\usepackage{goudysans}
%\usepackage{lmodern}
%\usepackage{libertine}
\usepackage{palatino}

\clubpenalty=10000
\widowpenalty=10000
\displaywidowpenalty=10000

% Listings (source code): a separate \lstloadlanguages and \lstset must be set up for each programming language. See example below.
\usepackage{listings}
\lstloadlanguages{Java}
\lstloadlanguages{python}
\lstset{%
	language=PHP,		 	 % language of the source code
	%numbers=left,           % line numbers on the left
	stepnumber=1,            % number every line.
	numbersep=5pt,           % 5pt spacing to the source code
	numberstyle=\tiny,       % 'tiny' font size for the numbers.
	breaklines=true,         % wrap lines if necessary.
	breakautoindent=true,    % indent the line after a line break.
	postbreak=\space,        % break at spaces.
	tabsize=2,               % tab size 2
	basicstyle=\ttfamily\footnotesize, % non-proportional font, small, for the source code
	showspaces=false,        % do not display spaces.
	showstringspaces=false,  % do not display spaces in strings ('') either.
	extendedchars=true,      % display all characters from the Latin1 character set.
	captionpos=b,            % sets the caption-position to bottom
	backgroundcolor=\color{ListingBackground} % set the background color of the source code.
}

% Glossary
\usepackage[
	nonumberlist, %do not display page numbers
	%section,     %appear at section level in the table of contents
	toc,          %entries in the table of contents
]{glossaries}

\usepackage{tocloft}

%Acronyms
\usepackage[printonlyused]{acronym}

% List of symbols
\usepackage{nomencl}
\renewcommand{\nomname}{List of Symbols}
\makenomenclature

% List of formulas
\usepackage{tocloft}
\newlistof{formulas}{for}{List of Formulas}

% List of algorithms
\usepackage{algorithm}
\usepackage{algpseudocode}
\floatname{algorithm}{Algorithm}
\renewcommand{\listalgorithmname}{List of Algorithms}

% List of listings
\usepackage{listings}
\renewcommand\lstlistingname{Listing}
\renewcommand\lstlistlistingname{List of Listings}


% Footnotes
\usepackage[perpage, hang, multiple, stable]{footmisc}

%Image path
\graphicspath{{images/}}

%only one LaTeX run needed to update the lists
\usepackage{bookmark}

%Images, tables, etc. can be placed exactly at the defined position with H
\usepackage{float}

% for vertical placement of text in tables
\usepackage{array}

% for displaying the Euro symbol
\usepackage[right]{eurosym}

% for text-wrapped graphics
\usepackage{wrapfig}

% a comment environment "k" (use with \begin{k}<comment text>\end{k},
% comments are printed in red). If the \% before excludecomment{k} is removed,
% comments are no longer printed.
\usepackage{comment}
\specialcomment{k}{\begingroup\color{red}}{\endgroup}
%\excludecomment{k}

% custom commands to reference chapters, sections, figures, and cross-references in general.
\newcommand{\refKap}[1]{Chapter \ref{#1} on page \pageref{#1} (\nameref{#1})}
\newcommand{\refSec}[1]{Section \ref{#1}}
\newcommand{\refAbb}[1]{Figure \ref{#1}}
\newcommand{\fullref}[1]{\ref{#1} on page \pageref{#1} (\nameref{#1})}

%moves numbers to the right
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyfoot[L]{\autor}
\fancyfoot[R]{\thepage} 
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0.6pt}

% Ensure page number appears bottom right even on plain pages
\fancypagestyle{plain}{
  \fancyhf{}
  \fancyfoot[L]{\autor}
  \fancyfoot[R]{\thepage}
  \renewcommand{\headrulewidth}{0pt}
    \renewcommand{\footrulewidth}{0.6pt}
}

% ============= Settings for the sizes of the various headings =============
\setkomafont{chapter}{\LARGE}
\setkomafont{section}{\Large}
\setkomafont{subsection}{\large}
\setkomafont{subsubsection}{\normalsize}
\setkomafont{paragraph}{\normalsize}
\setkomafont{subparagraph}{\small}


% Questionnaire
\usepackage{amssymb}     % For checkboxes (\square)
\usepackage{enumitem}    % For flexible lists


% Embed PDF
\usepackage{graphicx}
\usepackage{subcaption}


% Define a new list of appendices
\newlistof{anhangverzeichnis}{apc}{}
\newcommand{\anhang}[1]{
  \refstepcounter{chapter}
  \chapter*{Appendix \thechapter: #1}
  \addcontentsline{apc}{chapter}{Appendix \thechapter: #1}
}