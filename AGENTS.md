# t1000 – ERA Watch Data Analysis

## CRITICAL – FIRST STEP OF EVERY NEW SESSION
Before doing anything else: read `session-handoffs/LATEST.md`.
Treat its content as the current project state (latest findings, open
questions, next steps) before responding to the user's prompt. This is
not optional.

## Project Context
Data analysis component of a practical semester (Praxissemester) at
Emerson: analyzing ERA recording datasets (parquet, hive-partitioned) to
determine cycle timing and data fluency/quality, as part of a DHBW
Praxissemesterarbeit (T1000).

## Environment
- No admin rights on this device; working over SSH remote "flmc-ai" (Linux).
- Do NOT use system Python for parquet/pandas analysis — no pyarrow/pandas
  installed there. Use the working venv instead:
  `/home/ita/ERA-NAS/.venv/bin/python` (pyarrow 24.0.0, pandas 3.0.3).
- External data directories (read-only, outside this workspace — reference
  by absolute path, they are not auto-indexed since t1000 is the workspace
  root):
  - `/home/ita/ERA` — raw recording data, multiple `ERAP_EXT_00X` roots
  - `/home/ita/ERA-NAS` — stop-analysis tooling and reports
    (`run_stop_analysis.py`, `run_parallel_stop_analysis.sh`, `reports/`)
- Reuse existing docs in `/home/ita/ERA` instead of re-deriving:
  `V1_V2_DATA_COMPARISON.md`, `lazy_load_example.md` (lazy-loading patterns
  for large parquet exports — avoid eager loads that crash the kernel).

## Session Handoffs
- Location: `t1000/session-handoffs/`
- Naming: `handoff-YYYYMMDD-HHMMSS-<topic>.md`
- `session-handoffs/LATEST.md` is a symlink/pointer to the most recent
  handoff file — always read this one, not the raw folder listing.
- At the end of a session: write a new handoff file summarizing context,
  decisions, and open next steps, then update the `LATEST.md` pointer:
  ```bash
  ln -sf "$(ls -t session-handoffs/handoff-*.md | head -1)" session-handoffs/LATEST.md
  ```

## Output Conventions
- All files generated by Copilot for this thesis work go inside
  `t1000/` — never outside it (no write access to the parent directories).
- Scientific writing stack (for context, not generated here): LaTeX with
  biblatex/biber, verbose-footnote style, managed in VS Code + LaTeX
  Workshop, `.bib` sourced from Google Scholar / IEEE Xplore / Semantic
  Scholar. Every AI-suggested source must be manually verified against
  Google Scholar before use.